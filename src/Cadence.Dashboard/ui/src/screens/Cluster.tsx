import { Stack, Text, Title } from '@mantine/core'

export function Cluster() {
  return (
    <Stack gap="xs">
      <Title order={3}>Cluster</Title>
      <Text c="dimmed" size="sm">
        Instances and the pause switches arrive with task 11.
      </Text>
    </Stack>
  )
}
