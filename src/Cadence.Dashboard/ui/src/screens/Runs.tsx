import { Stack, Text, Title } from '@mantine/core'

export function Runs() {
  return (
    <Stack gap="xs">
      <Title order={3}>Runs</Title>
      <Text c="dimmed" size="sm">
        The run history table arrives with task 11.
      </Text>
    </Stack>
  )
}
